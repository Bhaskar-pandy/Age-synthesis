Loaded as API: https://robys01-face-aging.hf.space
Client.predict() Usage Info
---------------------------
Named API endpoints: 3

 - predict(image_path, source_age, target_age, api_name="/predict") -> aged_image
    Parameters:
     - [Image] image_path: dict(path: str | None (Path to a local file), url: str | None (Publicly available url or base64 encoded image), size: int | None (Size of image in bytes), orig_name: str | None (Original filename), mime_type: str | None (mime type of image), is_stream: bool (Can always be set to False), meta: dict()) (required)  (For input, either path or url must be provided. For output, path is always provided.) 
     - [Slider] source_age: float (not required, defaults to:   20)  
     - [Slider] target_age: float (not required, defaults to:   70)  
    Returns:
     - [Image] aged_image: dict(path: str | None (Path to a local file), url: str | None (Publicly available url or base64 encoded image), size: int | None (Size of image in bytes), orig_name: str | None (Original filename), mime_type: str | None (mime type of image), is_stream: bool (Can always be set to False), meta: dict()) 

 - predict(image_path, source_age, target_age, duration, fps, api_name="/predict_1") -> aged_video
    Parameters:
     - [Image] image_path: dict(path: str | None (Path to a local file), url: str | None (Publicly available url or base64 encoded image), size: int | None (Size of image in bytes), orig_name: str | None (Original filename), mime_type: str | None (mime type of image), is_stream: bool (Can always be set to False), meta: dict()) (required)  (For input, either path or url must be provided. For output, path is always provided.) 
     - [Slider] source_age: float (not required, defaults to:   20)  
     - [Slider] target_age: float (not required, defaults to:   70)  
     - [Slider] duration: float (not required, defaults to:   3)  
     - [Slider] fps: float (not required, defaults to:   30)  
    Returns:
     - [Video] aged_video: dict(video: filepath, subtitles: filepath | None) 

 - predict(image_path, source_age, api_name="/predict_2") -> aging_timelapse
    Parameters:
     - [Image] image_path: dict(path: str | None (Path to a local file), url: str | None (Publicly available url or base64 encoded image), size: int | None (Size of image in bytes), orig_name: str | None (Original filename), mime_type: str | None (mime type of image), is_stream: bool (Can always be set to False), meta: dict()) (required)  (For input, either path or url must be provided. For output, path is always provided.) 
     - [Slider] source_age: float (not required, defaults to:   20)  
    Returns:
     - [Video] aging_timelapse: dict(video: filepath, subtitles: filepath | None) 

